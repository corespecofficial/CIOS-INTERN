import test from "node:test";import assert from "node:assert/strict";import{attendanceClassification,canAccessProgrammeReport,csvEscape,paymentAllocations,performanceLevel,weightedPerformance}from"../src/lib/org-programme-rules.ts";
test("attendance windows classify Lagos meeting boundaries",()=>{const d=x=>new Date(`2026-07-14T${x}:00+01:00`);assert.equal(attendanceClassification(d("19:44"),d("19:45"),d("20:00"),d("20:15")),"not_open");assert.equal(attendanceClassification(d("19:45"),d("19:45"),d("20:00"),d("20:15")),"punctual");assert.equal(attendanceClassification(d("20:01"),d("19:45"),d("20:00"),d("20:15")),"late");assert.equal(attendanceClassification(d("20:16"),d("19:45"),d("20:00"),d("20:15")),"closed")});
test("weighted score and levels use required weights",()=>{assert.equal(weightedPerformance({attendance:100,assignments:100,approvedHours:100,roleOutput:100,growth:100,conduct:100}),100);assert.equal(performanceLevel(90),"distinguished");assert.equal(performanceLevel(59.99),"formal_intervention")});
test("payment allocations total exactly the payment",()=>{const r=paymentAllocations(100000);assert.deepEqual(r,{infrastructure:65000,delivery:20000,tax_admin:10000,performance_reserve:5000});assert.equal(Object.values(r).reduce((a,b)=>a+b,0),100000)});
test("CSV output prevents spreadsheet formula injection",()=>{assert.equal(csvEscape("=cmd()"),"'=cmd()");assert.equal(csvEscape('a,"b"'),'"a,""b"""')});
test("confidential report permissions cannot be bypassed",()=>{assert.equal(canAccessProgrammeReport("student","attendance"),false);assert.equal(canAccessProgrammeReport("instructor","finance"),false);assert.equal(canAccessProgrammeReport("finance","discipline"),false);assert.equal(canAccessProgrammeReport("finance","finance"),true);assert.equal(canAccessProgrammeReport("student","finance",true),true)});
