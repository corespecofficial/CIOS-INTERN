import{notFound}from"next/navigation";import{getAccountabilityDashboard}from"@/app/actions/org-performance-discipline";import{AccountabilityWorkspace}from"@/components/org-operations/accountability-workspace";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{orgSlug:string}>}){const{orgSlug}=await params,r=await getAccountabilityDashboard(orgSlug);if(!r.ok)notFound();return <div style={{maxWidth:1180}}><h1 style={{margin:"0 0 4px",fontSize:26}}>My Performance</h1><p style={{color:"#8892A4",marginTop:0}}>Your published scorecards, formal notices and right to respond.</p><AccountabilityWorkspace orgSlug={orgSlug}{...r.data}/></div>}
