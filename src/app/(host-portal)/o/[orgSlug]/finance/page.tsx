import{notFound}from"next/navigation";import{getFinanceDashboard}from"@/app/actions/org-finance";import{FinanceWorkspace}from"@/components/org-operations/finance-workspace";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{orgSlug:string}>}){const{orgSlug}=await params,r=await getFinanceDashboard(orgSlug);if(!r.ok)notFound();return <div style={{maxWidth:1180}}><h1 style={{margin:"0 0 4px",fontSize:26}}>Finance & Bookkeeping</h1><p style={{color:"#8892A4",marginTop:0}}>Clients, receivables, Flutterwave payments, allocations and verified expenses.</p><FinanceWorkspace orgSlug={orgSlug}{...r.data}/></div>}
